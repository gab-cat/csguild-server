<html>
  <head>
    <meta charset='utf-8' />
    <meta name='viewport' content='width=device-width, initial-scale=1.0' />
    <title>Email Verification - CS Guild</title>
    <style>
      @import url('https://fonts.googleapis.com/css2?family=Inter:wght@300;400;500;600;700&family=JetBrains+Mono:wght@400;500;600&display=swap');

      * {
        margin: 0;
        padding: 0;
        box-sizing: border-box;
      }

      body {
        font-family:
          'Inter',
          -apple-system,
          BlinkMacSystemFont,
          'Segoe UI',
          Roboto,
          sans-serif;
        line-height: 1.6;
        color: #ffffff;
        background: linear-gradient(135deg, #0f0f23 0%, #1a1a2e 100%);
        padding: 16px;
        min-height: 100vh;
      }

      .container {
        max-width: 600px;
        margin: 0 auto;
        background: linear-gradient(135deg, #1a1a2e 0%, #16213e 100%);
        border-radius: 12px;
        overflow: hidden;
        box-shadow: 0 8px 32px rgba(0, 0, 0, 0.3);
        border: 1px solid rgba(139, 92, 246, 0.2);
      }

      .header {
        background: linear-gradient(135deg, #ec4899 0%, #8b5cf6 100%);
        padding: 24px 32px 20px;
        position: relative;
        overflow: hidden;
      }

      .header::before {
        content: '';
        position: absolute;
        top: 0;
        left: 0;
        right: 0;
        bottom: 0;
        background: linear-gradient(
          90deg,
          transparent 0%,
          rgba(255, 255, 255, 0.1) 50%,
          transparent 100%
        );
        transform: translateX(-100%);
        animation: shine 3s ease-in-out infinite;
      }

      @keyframes shine {
        0% {
          transform: translateX(-100%);
        }
        50% {
          transform: translateX(100%);
        }
        100% {
          transform: translateX(100%);
        }
      }

      @keyframes gradientShift {
        0%,
        100% {
          background-position: 0% 50%;
        }
        50% {
          background-position: 100% 50%;
        }
      }

      .logo {
        font-size: 24px;
        font-weight: 700;
        color: #ffffff;
        margin-bottom: 6px;
        display: flex;
        align-items: center;
        gap: 10px;
        font-family: 'JetBrains Mono', monospace;
      }

      .logo::before {
        content: '</>';
        font-size: 20px;
        background: linear-gradient(45deg, #f472b6, #a855f7);
        -webkit-background-clip: text;
        -webkit-text-fill-color: transparent;
        background-clip: text;
      }

      .header h1 {
        color: rgba(255, 255, 255, 0.9);
        font-size: 16px;
        font-weight: 400;
        margin: 0;
        font-family: 'JetBrains Mono', monospace;
      }

      .header h1::before {
        content: '// ';
        color: rgba(255, 255, 255, 0.6);
      }

      .content {
        padding: 32px;
      }

      .greeting {
        font-size: 18px;
        font-weight: 500;
        color: #ffffff;
        margin-bottom: 20px;
      }

      .description {
        font-size: 16px;
        color: rgba(255, 255, 255, 0.8);
        margin-bottom: 28px;
        line-height: 1.6;
      }

      .verification-section {
        background: linear-gradient(
          135deg,
          rgba(236, 72, 153, 0.15) 0%,
          rgba(139, 92, 246, 0.15) 100%
        );
        border: 1px solid rgba(236, 72, 153, 0.3);
        border-radius: 12px;
        padding: 32px 24px;
        text-align: center;
        margin: 24px 0;
        position: relative;
        overflow: hidden;
      }

      .verification-section::before {
        content: '';
        position: absolute;
        top: 0;
        left: 0;
        right: 0;
        height: 2px;
        background: linear-gradient(90deg, #ec4899, #8b5cf6, #a855f7, #ec4899);
        background-size: 300% 100%;
        animation: gradientShift 3s ease infinite;
      }

      .verification-label {
        font-size: 12px;
        font-weight: 500;
        color: rgba(236, 72, 153, 0.8);
        text-transform: uppercase;
        letter-spacing: 1px;
        margin-bottom: 16px;
        font-family: 'JetBrains Mono', monospace;
      }

      .verification-label::before {
        content: '# ';
        color: rgba(139, 92, 246, 0.7);
      }

      .verification-code {
        font-size: 42px;
        font-weight: 700;
        letter-spacing: 10px;
        font-family: 'JetBrains Mono', monospace;
        background: linear-gradient(45deg, #ec4899, #8b5cf6, #a855f7);
        -webkit-background-clip: text;
        -webkit-text-fill-color: transparent;
        background-clip: text;
        margin-bottom: 16px;
        animation: pulse 2s ease-in-out infinite;
      }

      @keyframes pulse {
        0%,
        100% {
          transform: scale(1);
        }
        50% {
          transform: scale(1.02);
        }
      }

      .expiry-notice {
        font-size: 14px;
        color: rgba(255, 255, 255, 0.7);
        display: flex;
        align-items: center;
        justify-content: center;
        gap: 6px;
        margin-top: 12px;
      }

      .expiry-notice::before {
        content: '⏰';
      }

      .security-note {
        background: linear-gradient(
          135deg,
          rgba(245, 158, 11, 0.15) 0%,
          rgba(217, 119, 6, 0.15) 100%
        );
        border-left: 3px solid #f59e0b;
        padding: 16px;
        margin: 20px 0;
        border-radius: 6px;
        font-size: 14px;
        color: rgba(245, 158, 11, 0.9);
        border: 1px solid rgba(245, 158, 11, 0.2);
      }

      .security-note::before {
        content: '⚠️';
        margin-right: 6px;
      }

      .footer {
        background: linear-gradient(
          135deg,
          rgba(31, 41, 55, 0.8) 0%,
          rgba(17, 24, 39, 0.8) 100%
        );
        padding: 24px 32px;
        text-align: center;
        border-top: 1px solid rgba(139, 92, 246, 0.2);
      }

      .footer-brand {
        font-size: 16px;
        font-weight: 700;
        color: #ffffff;
        margin-bottom: 6px;
        font-family: 'JetBrains Mono', monospace;
      }

      .footer-brand::before {
        content: '// ';
        color: rgba(236, 72, 153, 0.8);
      }

      .footer-tagline {
        font-size: 13px;
        color: rgba(255, 255, 255, 0.7);
        margin-bottom: 16px;
      }

      .footer-note {
        font-size: 11px;
        color: rgba(255, 255, 255, 0.5);
        font-family: 'JetBrains Mono', monospace;
      }

      .cta-section {
        background: linear-gradient(
          135deg,
          rgba(139, 92, 246, 0.1) 0%,
          rgba(168, 85, 247, 0.1) 100%
        );
        border: 1px solid rgba(139, 92, 246, 0.2);
        border-radius: 12px;
        padding: 24px;
        margin: 24px 0;
        text-align: center;
      }

      .cta-title {
        font-size: 18px;
        font-weight: 600;
        color: #ffffff;
        margin-bottom: 12px;
      }

      .cta-desc {
        font-size: 15px;
        color: rgba(255, 255, 255, 0.8);
        margin-bottom: 20px;
        line-height: 1.5;
      }

      .cta-button {
        display: inline-block;
        background: linear-gradient(135deg, #ec4899 0%, #8b5cf6 100%);
        color: #ffffff;
        padding: 12px 28px;
        border-radius: 6px;
        text-decoration: none;
        font-weight: 600;
        transition: all 0.3s ease;
        box-shadow: 0 3px 12px rgba(236, 72, 153, 0.3);
      }

      .cta-button:hover {
        transform: translateY(-1px);
        box-shadow: 0 6px 20px rgba(236, 72, 153, 0.4);
      }

      @media (max-width: 768px) {
        .container {
          margin: 8px;
          border-radius: 10px;
        }

        .content {
          padding: 20px;
        }

        .verification-section {
          padding: 24px 16px;
        }

        .verification-code {
          font-size: 32px;
          letter-spacing: 6px;
        }

        .header {
          padding: 20px 16px 16px;
        }

        .footer {
          padding: 20px 16px;
        }
      }
    </style>
  </head>
  <body>
    <div class='container'>
      <div class='header'>
        <div class='logo'>CS Guild</div>
        <h1>Secure Email Verification</h1>
      </div>

      <div class='content'>
        <p class='greeting'>Hey {{firstName}},</p>

        <p class='description'>
          Welcome to
          {{organizationName}}! To complete your account setup and join our
          community, please verify your email address using the verification
          code below.
        </p>

        <div class='verification-section'>
          <div class='verification-label'>Verification Code</div>
          <div class='verification-code'>{{verificationCode}}</div>
          <div class='expiry-notice'>
            Please use this code to verify your email address
          </div>
        </div>

        <div class='security-note'>
          <strong>Security Notice:</strong>
          This verification code is unique to your account. Never share it with
          anyone.
          {{organizationName}}
          staff will never ask for your verification code.
        </div>

        <div class='cta-section'>
          <h3 class='cta-title'>Complete Your Journey</h3>
          <p class='cta-desc'>
            Once verified, you'll have access to all the features and benefits
            of
            {{organizationName}}.
          </p>
        </div>

        <p class='description'>
          Having trouble? Our community is here to help! Reach out to our
          support team at
          {{supportEmail}}
          or check out our troubleshooting guide in the help center.
        </p>
      </div>

      <div class='footer'>
        <div class='footer-brand'>Built with ❤️ by {{organizationName}}</div>
        <p class='footer-tagline'>Where Code Meets Community</p>
        <p class='footer-note'>
          if (needHelp) { contact("{{supportEmail}}"); }
        </p>
      </div>
    </div>
  </body>
</html>