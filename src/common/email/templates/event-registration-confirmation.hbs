<html>
  <head>
    <meta charset='utf-8' />
    <meta name='viewport' content='width=device-width, initial-scale=1.0' />
    <title>Event Registration Confirmed</title>
    <style>
      @import url('https://fonts.googleapis.com/css2?family=Inter:wght@300;400;500;600;700&family=JetBrains+Mono:wght@400;500;600&display=swap');

      * {
        margin: 0;
        padding: 0;
        box-sizing: border-box;
      }

      body {
        font-family:
          'Inter',
          -apple-system,
          BlinkMacSystemFont,
          'Segoe UI',
          Roboto,
          sans-serif;
        line-height: 1.6;
        color: #ffffff;
        background: linear-gradient(135deg, #0f0f23 0%, #1a1a2e 100%);
        padding: 16px;
        min-height: 100vh;
      }

      .container {
        max-width: 700px;
        margin: 0 auto;
        background: linear-gradient(135deg, #1a1a2e 0%, #16213e 100%);
        border-radius: 12px;
        overflow: hidden;
        box-shadow: 0 8px 32px rgba(0, 0, 0, 0.3);
        border: 1px solid rgba(139, 92, 246, 0.2);
      }

      .header {
        background: linear-gradient(135deg, #10b981 0%, #3b82f6 100%);
        padding: 24px 32px 20px;
        position: relative;
        overflow: hidden;
      }

      .header::before {
        content: '';
        position: absolute;
        top: 0;
        left: 0;
        right: 0;
        bottom: 0;
        background: linear-gradient(
          90deg,
          transparent 0%,
          rgba(255, 255, 255, 0.1) 50%,
          transparent 100%
        );
        transform: translateX(-100%);
        animation: shine 3s ease-in-out infinite;
      }

      @keyframes shine {
        0% {
          transform: translateX(-100%);
        }
        50% {
          transform: translateX(100%);
        }
        100% {
          transform: translateX(100%);
        }
      }

      .logo {
        font-size: 24px;
        font-weight: 700;
        color: #ffffff;
        margin-bottom: 6px;
        display: flex;
        align-items: center;
        gap: 10px;
        font-family: 'JetBrains Mono', monospace;
      }

      .logo::before {
        content: '</>';
        font-size: 20px;
        background: linear-gradient(45deg, #34d399, #60a5fa);
        -webkit-background-clip: text;
        -webkit-text-fill-color: transparent;
        background-clip: text;
      }

      .header h1 {
        color: rgba(255, 255, 255, 0.9);
        font-size: 16px;
        font-weight: 400;
        margin: 0;
        font-family: 'JetBrains Mono', monospace;
      }

      .header h1::before {
        content: '// ';
        color: rgba(255, 255, 255, 0.6);
      }

      .content {
        padding: 32px;
      }

      .confirmation-hero {
        background: linear-gradient(
          135deg,
          rgba(16, 185, 129, 0.15) 0%,
          rgba(59, 130, 246, 0.15) 100%
        );
        border: 1px solid rgba(16, 185, 129, 0.3);
        border-radius: 12px;
        padding: 32px;
        margin-bottom: 24px;
        text-align: center;
        position: relative;
        overflow: hidden;
      }

      .confirmation-hero::before {
        content: '';
        position: absolute;
        top: 0;
        left: 0;
        right: 0;
        height: 2px;
        background: linear-gradient(90deg, #10b981, #3b82f6, #06b6d4, #10b981);
        background-size: 300% 100%;
        animation: confirmationShift 3s ease infinite;
      }

      @keyframes confirmationShift {
        0%,
        100% {
          background-position: 0% 50%;
        }
        50% {
          background-position: 100% 50%;
        }
      }

      .confirmation-icon {
        font-size: 64px;
        margin-bottom: 20px;
        display: block;
        animation: bounce 2s ease infinite;
      }

      @keyframes bounce {
        0%,
        20%,
        50%,
        80%,
        100% {
          transform: translateY(0);
        }
        40% {
          transform: translateY(-10px);
        }
        60% {
          transform: translateY(-5px);
        }
      }

      .confirmation-title {
        font-size: 28px;
        font-weight: 700;
        background: linear-gradient(45deg, #10b981, #3b82f6, #06b6d4);
        -webkit-background-clip: text;
        -webkit-text-fill-color: transparent;
        background-clip: text;
        margin-bottom: 12px;
        line-height: 1.2;
      }

      .confirmation-subtitle {
        font-size: 18px;
        color: rgba(16, 185, 129, 0.9);
        font-weight: 500;
        font-family: 'JetBrains Mono', monospace;
      }

      .greeting {
        font-size: 18px;
        font-weight: 500;
        color: #ffffff;
        margin-bottom: 20px;
      }

      .description {
        font-size: 15px;
        color: rgba(255, 255, 255, 0.8);
        margin-bottom: 24px;
        line-height: 1.6;
      }

      .event-details {
        background: linear-gradient(
          135deg,
          rgba(31, 41, 55, 0.6) 0%,
          rgba(55, 65, 81, 0.6) 100%
        );
        border: 1px solid rgba(16, 185, 129, 0.3);
        border-radius: 12px;
        padding: 24px;
        margin: 24px 0;
        position: relative;
        overflow: hidden;
        backdrop-filter: blur(10px);
      }

      .event-details::before {
        content: '';
        position: absolute;
        top: 0;
        left: 0;
        right: 0;
        height: 2px;
        background: linear-gradient(90deg, #10b981, #3b82f6, #06b6d4, #10b981);
        background-size: 300% 100%;
        animation: eventShift 4s ease infinite;
      }

      @keyframes eventShift {
        0%,
        100% {
          background-position: 0% 50%;
        }
        50% {
          background-position: 100% 50%;
        }
      }

      .event-details h3 {
        font-size: 20px;
        font-weight: 600;
        color: #ffffff;
        margin-bottom: 16px;
        display: flex;
        align-items: center;
        gap: 8px;
      }

      .event-details h3::before {
        content: '🎉';
        font-size: 18px;
      }

      .event-title {
        font-size: 24px;
        font-weight: 700;
        background: linear-gradient(45deg, #10b981, #3b82f6);
        -webkit-background-clip: text;
        -webkit-text-fill-color: transparent;
        background-clip: text;
        margin-bottom: 20px;
        line-height: 1.3;
      }

      .detail-grid {
        display: grid;
        grid-template-columns: repeat(auto-fit, minmax(200px, 1fr));
        gap: 16px;
      }

      .detail-item {
        background: rgba(31, 41, 55, 0.5);
        border: 1px solid rgba(16, 185, 129, 0.2);
        border-radius: 8px;
        padding: 16px;
      }

      .detail-label {
        font-size: 12px;
        font-weight: 500;
        color: rgba(16, 185, 129, 0.8);
        text-transform: uppercase;
        letter-spacing: 1px;
        margin-bottom: 8px;
        font-family: 'JetBrains Mono', monospace;
      }

      .detail-value {
        font-size: 15px;
        font-weight: 600;
        color: #ffffff;
        line-height: 1.4;
      }

      .detail-value.highlight {
        font-size: 16px;
        background: linear-gradient(45deg, #10b981, #3b82f6);
        -webkit-background-clip: text;
        -webkit-text-fill-color: transparent;
        background-clip: text;
      }

      .next-steps {
        background: linear-gradient(
          135deg,
          rgba(59, 130, 246, 0.1) 0%,
          rgba(6, 182, 212, 0.1) 100%
        );
        border: 1px solid rgba(59, 130, 246, 0.2);
        border-radius: 12px;
        padding: 24px;
        margin: 24px 0;
      }

      .next-steps h3 {
        font-size: 18px;
        font-weight: 600;
        color: #ffffff;
        margin-bottom: 16px;
        display: flex;
        align-items: center;
        gap: 8px;
      }

      .next-steps h3::before {
        content: '📅';
        font-size: 16px;
      }

      .steps-list {
        list-style: none;
        padding: 0;
        margin: 0;
      }

      .steps-list li {
        display: flex;
        align-items: flex-start;
        gap: 12px;
        margin-bottom: 12px;
        padding: 12px;
        background: rgba(31, 41, 55, 0.3);
        border-radius: 8px;
        border: 1px solid rgba(59, 130, 246, 0.2);
      }

      .step-number {
        background: linear-gradient(45deg, #3b82f6, #06b6d4);
        color: #ffffff;
        width: 24px;
        height: 24px;
        border-radius: 50%;
        display: flex;
        align-items: center;
        justify-content: center;
        font-size: 12px;
        font-weight: 600;
        flex-shrink: 0;
      }

      .step-text {
        font-size: 14px;
        color: rgba(255, 255, 255, 0.9);
        line-height: 1.4;
      }

      .cta-section {
        background: linear-gradient(
          135deg,
          #10b981 0%,
          #3b82f6 50%,
          #06b6d4 100%
        );
        border-radius: 12px;
        padding: 24px;
        text-align: center;
        margin: 24px 0;
        color: #ffffff;
        position: relative;
        overflow: hidden;
      }

      .cta-section::before {
        content: '';
        position: absolute;
        top: 0;
        left: 0;
        right: 0;
        bottom: 0;
        background: linear-gradient(
          45deg,
          transparent 30%,
          rgba(255, 255, 255, 0.1) 50%,
          transparent 70%
        );
        transform: translateX(-100%);
        animation: ctaShine 4s ease-in-out infinite;
      }

      @keyframes ctaShine {
        0% {
          transform: translateX(-100%);
        }
        50% {
          transform: translateX(100%);
        }
        100% {
          transform: translateX(100%);
        }
      }

      .cta-title {
        font-size: 20px;
        font-weight: 700;
        margin-bottom: 10px;
      }

      .cta-desc {
        font-size: 15px;
        opacity: 0.9;
        margin-bottom: 20px;
      }

      .cta-button {
        display: inline-block;
        background: rgba(255, 255, 255, 0.2);
        color: #ffffff;
        padding: 12px 32px;
        border-radius: 8px;
        text-decoration: none;
        font-weight: 600;
        font-size: 16px;
        border: 2px solid rgba(255, 255, 255, 0.3);
        transition: all 0.3s ease;
        backdrop-filter: blur(10px);
      }

      .cta-button:hover {
        background: rgba(255, 255, 255, 0.3);
        border-color: rgba(255, 255, 255, 0.5);
        transform: translateY(-2px);
      }

      .footer {
        background: linear-gradient(
          135deg,
          rgba(31, 41, 55, 0.8) 0%,
          rgba(17, 24, 39, 0.8) 100%
        );
        padding: 24px 32px;
        text-align: center;
        border-top: 1px solid rgba(16, 185, 129, 0.2);
      }

      .footer-brand {
        font-size: 16px;
        font-weight: 700;
        color: #ffffff;
        margin-bottom: 6px;
        font-family: 'JetBrains Mono', monospace;
      }

      .footer-brand::before {
        content: '// ';
        color: rgba(16, 185, 129, 0.8);
      }

      .footer-tagline {
        font-size: 13px;
        color: rgba(255, 255, 255, 0.7);
        margin-bottom: 16px;
      }

      .footer-links {
        display: flex;
        justify-content: center;
        gap: 20px;
        margin-bottom: 16px;
        flex-wrap: wrap;
      }

      .footer-link {
        color: rgba(16, 185, 129, 0.8);
        text-decoration: none;
        font-size: 13px;
        font-weight: 500;
        transition: color 0.3s ease;
      }

      .footer-link:hover {
        color: #10b981;
      }

      .footer-note {
        font-size: 11px;
        color: rgba(255, 255, 255, 0.5);
        font-family: 'JetBrains Mono', monospace;
      }

      @media (max-width: 768px) {
        .container {
          margin: 8px;
          border-radius: 10px;
        }

        .content {
          padding: 20px;
        }

        .confirmation-hero {
          padding: 24px 16px;
        }

        .confirmation-title {
          font-size: 22px;
        }

        .event-title {
          font-size: 20px;
        }

        .detail-grid {
          grid-template-columns: 1fr;
        }

        .footer-links {
          flex-direction: column;
          gap: 10px;
        }
      }
    </style>
  </head>
  <body>
    <div class='container'>
      <div class='header'>
        <div class='logo'>CS Guild</div>
        <h1>Event Registration System</h1>
      </div>

      <div class='content'>
        <div class='confirmation-hero'>
          <span class='confirmation-icon'>✅</span>
          <h2 class='confirmation-title'>Registration Confirmed!</h2>
          <p class='confirmation-subtitle'>You're all set for the event</p>
        </div>

        <p class='greeting'>Hey {{firstName}},</p>

        <p class='description'>
          Great news! Your registration for the upcoming event has been
          confirmed. We're excited to have you join us for this amazing
          experience.
        </p>

        <div class='event-details'>
          <h3>Event Details</h3>
          <div class='event-title'>{{eventTitle}}</div>
          <div class='detail-grid'>
            <div class='detail-item'>
              <div class='detail-label'>Start Date & Time</div>
              <div class='detail-value highlight'>
                {{eventStartDate}}<br />
                {{eventStartTime}}
              </div>
            </div>
            {{#if eventEndDate}}
              <div class='detail-item'>
                <div class='detail-label'>End Date & Time</div>
                <div class='detail-value highlight'>
                  {{eventEndDate}}<br />
                  {{eventEndTime}}
                </div>
              </div>
            {{/if}}
            <div class='detail-item'>
              <div class='detail-label'>Registered User</div>
              <div class='detail-value'>
                {{firstName}}<br />
                <span
                  style='color: rgba(255, 255, 255, 0.7); font-size: 13px;'
                >@{{username}}</span>
              </div>
            </div>
            <div class='detail-item'>
              <div class='detail-label'>Event Link</div>
              <div class='detail-value'>
                <a
                  href='{{eventUrl}}'
                  style='color: #10b981; text-decoration: none; font-weight: 600;'
                >
                  View Event Details
                </a>
              </div>
            </div>
          </div>
        </div>

        <div class='next-steps'>
          <h3>What's Next?</h3>
          <ul class='steps-list'>
            <li>
              <div class='step-number'>1</div>
              <div class='step-text'>
                <strong>Save the Date:</strong>
                Add this event to your calendar so you don't miss it!
              </div>
            </li>
            <li>
              <div class='step-number'>2</div>
              <div class='step-text'>
                <strong>Event Preparation:</strong>
                Check the event details for any required materials or
                preparation.
              </div>
            </li>
            <li>
              <div class='step-number'>3</div>
              <div class='step-text'>
                <strong>Stay Updated:</strong>
                Watch for event updates and reminders in your email.
              </div>
            </li>
            <li>
              <div class='step-number'>4</div>
              <div class='step-text'>
                <strong>Join the Community:</strong>
                Connect with other attendees and get ready for an amazing
                experience!
              </div>
            </li>
          </ul>
        </div>

        <div class='cta-section'>
          <h3 class='cta-title'>Ready for the Event?</h3>
          <p class='cta-desc'>
            Visit the event page to see more details, connect with other
            attendees, and get the latest updates.
          </p>
          <a href='{{eventUrl}}' class='cta-button'>View Event Details</a>
        </div>

        <p class='description'>
          <strong>Need Help?</strong>
          If you have any questions about the event or need to make changes to
          your registration, don't hesitate to reach out to our support team at
          {{supportEmail}}.
        </p>

        <p class='description'>
          We can't wait to see you at the event! Get ready for an incredible
          experience with the
          {{organizationName}}
          community.
        </p>
      </div>

      <div class='footer'>
        <div class='footer-brand'>{{organizationName}} Events</div>
        <p class='footer-tagline'>Building connections through amazing
          experiences</p>

        <div class='footer-links'>
          <a href='{{eventUrl}}' class='footer-link'>Event Details</a>
          <a href='{{frontendUrl}}' class='footer-link'>Dashboard</a>
        </div>

        <p class='footer-note'>
          console.log("See you at the event! 🎉");
        </p>
      </div>
    </div>
  </body>
</html>