<html>
  <head>
    <meta charset='utf-8' />
    <meta name='viewport' content='width=device-width, initial-scale=1.0' />
    <title>Password Reset - {{organizationName}}</title>
    <style>
      @import url('https://fonts.googleapis.com/css2?family=Inter:wght@300;400;500;600;700&family=JetBrains+Mono:wght@400;500;600&display=swap');

      * {
        margin: 0;
        padding: 0;
        box-sizing: border-box;
      }

      body {
        font-family:
          'Inter',
          -apple-system,
          BlinkMacSystemFont,
          'Segoe UI',
          Roboto,
          sans-serif;
        line-height: 1.6;
        color: #1f2937;
        background-color: #f8fafc;
        padding: 20px;
        min-height: 100vh;
      }

      .container {
        max-width: 800px;
        margin: 0 auto;
        background: #ffffff;
        border-radius: 16px;
        overflow: hidden;
        box-shadow: 0 4px 6px -1px rgba(0, 0, 0, 0.1);
        border: 1px solid #e5e7eb;
      }

      .header {
        background: #ffffff;
        padding: 32px 40px 24px;
        border-bottom: 1px solid #e5e7eb;
        position: relative;
      }

      .header::after {
        content: '';
        position: absolute;
        bottom: 0;
        left: 0;
        right: 0;
        height: 3px;
        background: linear-gradient(90deg, #dc2626, #ef4444, #f97316, #dc2626);
        background-size: 300% 100%;
        animation: alertShift 2s ease infinite;
      }

      @keyframes alertShift {
        0%,
        100% {
          background-position: 0% 50%;
        }
        50% {
          background-position: 100% 50%;
        }
      }

      .logo {
        font-size: 24px;
        font-weight: 700;
        color: #1f2937;
        margin-bottom: 8px;
        display: flex;
        align-items: center;
        gap: 8px;
      }

      .logo::before {
        content: '🔐';
        font-size: 20px;
      }

      .header h1 {
        color: #4b5563;
        font-size: 18px;
        font-weight: 400;
        margin: 0;
      }

      .content {
        padding: 40px;
      }

      .alert-banner {
        background: linear-gradient(135deg, #fef3c7 0%, #fde68a 100%);
        border: 1px solid #f59e0b;
        border-radius: 12px;
        padding: 24px;
        margin-bottom: 32px;
        text-align: center;
        position: relative;
        overflow: hidden;
      }

      .alert-banner::before {
        content: '';
        position: absolute;
        top: 0;
        left: 0;
        right: 0;
        height: 3px;
        background: linear-gradient(90deg, #f59e0b, #f97316, #dc2626, #f59e0b);
        background-size: 300% 100%;
        animation: alertShift 2s ease infinite;
      }

      .alert-icon {
        font-size: 48px;
        margin-bottom: 16px;
        display: block;
      }

      .alert-title {
        font-size: 20px;
        font-weight: 600;
        color: #92400e;
        margin-bottom: 8px;
      }

      .alert-description {
        font-size: 16px;
        color: #a16207;
      }

      .greeting {
        font-size: 18px;
        font-weight: 500;
        color: #1f2937;
        margin-bottom: 24px;
      }

      .description {
        font-size: 16px;
        color: #4b5563;
        margin-bottom: 32px;
        line-height: 1.7;
      }

      .reset-section {
        background: linear-gradient(135deg, #fef2f2 0%, #fee2e2 100%);
        border: 1px solid #fecaca;
        border-radius: 12px;
        padding: 32px;
        text-align: center;
        margin: 32px 0;
        position: relative;
        overflow: hidden;
      }

      .reset-section::before {
        content: '';
        position: absolute;
        top: 0;
        left: 0;
        right: 0;
        height: 3px;
        background: linear-gradient(90deg, #dc2626, #ef4444, #f97316, #dc2626);
        background-size: 300% 100%;
        animation: resetShift 3s ease infinite;
      }

      @keyframes resetShift {
        0%,
        100% {
          background-position: 0% 50%;
        }
        50% {
          background-position: 100% 50%;
        }
      }

      .reset-label {
        font-size: 14px;
        font-weight: 500;
        color: #6b7280;
        text-transform: uppercase;
        letter-spacing: 0.5px;
        margin-bottom: 16px;
      }

      .reset-code {
        font-size: 36px;
        font-weight: 600;
        color: #dc2626;
        letter-spacing: 8px;
        font-family: 'JetBrains Mono', 'Courier New', monospace;
        background: linear-gradient(45deg, #dc2626, #ef4444);
        -webkit-background-clip: text;
        -webkit-text-fill-color: transparent;
        background-clip: text;
        margin-bottom: 16px;
      }

      .expiry-notice {
        font-size: 14px;
        color: #6b7280;
        display: flex;
        align-items: center;
        justify-content: center;
        gap: 8px;
      }

      .expiry-notice::before {
        content: '⏰';
      }

      .reset-button {
        display: inline-block;
        padding: 16px 32px;
        background: linear-gradient(135deg, #dc2626 0%, #ef4444 100%);
        color: white;
        text-decoration: none;
        border-radius: 12px;
        font-weight: 600;
        font-size: 16px;
        margin: 24px 0;
        transition: all 0.3s ease;
        box-shadow: 0 4px 15px rgba(220, 38, 38, 0.2);
      }

      .reset-button:hover {
        transform: translateY(-2px);
        box-shadow: 0 8px 25px rgba(220, 38, 38, 0.3);
      }

      .security-info {
        background: linear-gradient(135deg, #f0f9ff 0%, #e0f2fe 100%);
        border-left: 4px solid #0284c7;
        padding: 24px;
        margin: 24px 0;
        border-radius: 8px;
        font-size: 15px;
        color: #0c4a6e;
      }

      .security-info::before {
        content: '🔒';
        margin-right: 8px;
        font-size: 18px;
      }

      .security-info h3 {
        font-size: 16px;
        font-weight: 600;
        margin-bottom: 12px;
        color: #0c4a6e;
      }

      .security-info ul {
        margin-left: 20px;
        line-height: 1.6;
      }

      .security-info li {
        margin-bottom: 8px;
      }

      .warning-text {
        font-size: 15px;
        color: #4b5563;
        margin: 24px 0;
        line-height: 1.7;
      }

      .footer {
        background: #f8fafc;
        padding: 32px 40px;
        text-align: center;
        border-top: 1px solid #e5e7eb;
      }

      .footer-brand {
        font-size: 16px;
        font-weight: 600;
        color: #1f2937;
        margin-bottom: 8px;
      }

      .footer-tagline {
        font-size: 14px;
        color: #6b7280;
        margin-bottom: 16px;
      }

      .support-link {
        color: #dc2626;
        text-decoration: none;
        font-weight: 500;
        transition: color 0.2s ease;
      }

      .support-link:hover {
        color: #991b1b;
      }

      @media (max-width: 768px) {
        body {
          padding: 10px;
        }

        .container {
          border-radius: 12px;
          max-width: 100%;
        }

        .header {
          padding: 24px 20px 20px;
        }

        .content {
          padding: 24px 20px;
        }

        .footer {
          padding: 24px 20px;
        }

        .reset-code {
          font-size: 28px;
          letter-spacing: 4px;
        }

        .reset-button {
          padding: 14px 24px;
          font-size: 15px;
        }

        .logo {
          font-size: 20px;
        }

        .header h1 {
          font-size: 16px;
        }
      }

      @media (max-width: 480px) {
        .reset-code {
          font-size: 24px;
          letter-spacing: 2px;
        }

        .reset-section {
          padding: 24px 16px;
        }
      }

      @media (min-width: 1024px) {
        .container {
          max-width: 900px;
        }

        .content {
          padding: 48px 60px;
        }

        .header {
          padding: 40px 60px 32px;
        }

        .footer {
          padding: 40px 60px;
        }
      }
    </style>
  </head>
  <body>
    <div class='container'>
      <div class='header'>
        <div class='logo'>{{organizationName}}</div>
        <h1>Password Reset Request</h1>
      </div>

      <div class='content'>
        <div class='alert-banner'>
          <div class='alert-icon'>🔐</div>
          <div class='alert-title'>Security Alert</div>
          <div class='alert-description'>A password reset was requested for your
            account</div>
        </div>

        <div class='greeting'>Hello {{firstName}},</div>

        <div class='description'>
          We received a request to reset your password for your
          {{organizationName}}
          account. If you made this request, please use the secure reset code
          below to create a new password.
        </div>

        <div class='reset-section'>
          <div class='reset-label'>Reset Code</div>
          <div class='reset-code'>{{resetCode}}</div>
          <div class='expiry-notice'>Expires in 1 hour</div>
        </div>

        <div style='text-align: center;'>
          <a href='{{resetUrl}}?code={{resetCode}}' class='reset-button'>
            Reset Password Securely
          </a>
        </div>

        <div class='security-info'>
          <h3>Security Information</h3>
          <ul>
            <li>This reset code will expire in 1 hour for your security</li>
            <li>Only use this code if you requested a password reset</li>
            <li>Never share this code with anyone</li>
            <li>If you suspect unauthorized access, contact support immediately</li>
          </ul>
        </div>

        <div class='warning-text'>
          <strong>Important:</strong>
          If you did not request a password reset, please ignore this email and
          your password will remain unchanged. Consider enabling two-factor
          authentication for enhanced security.
        </div>
      </div>

      <div class='footer'>
        <div class='footer-brand'>The {{organizationName}} Team</div>
        <div class='footer-tagline'>Securing your digital future</div>
        <div>
          Security concerns? Contact us at
          <a
            href='mailto:{{supportEmail}}'
            class='support-link'
          >{{supportEmail}}</a>
        </div>
      </div>
    </div>
  </body>
</html>